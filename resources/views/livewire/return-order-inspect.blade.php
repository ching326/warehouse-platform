<div><form wire:submit="saveInspect" class="form-stack"><section class="table-shell flux-panel form-panel"><div class="form-panel-header"><strong>{{ __('return_orders.inspect_page_title') }}</strong></div>@foreach($order->lines as $line)<div class="form-grid three"><div><strong>{{ $line->sku?->sku }}</strong><span class="subtle">{{ $line->stockItem?->code }}</span></div><flux:select wire:model="lineDrafts.{{ $line->id }}.condition" :label="__('return_orders.field_condition')">@foreach($conditions as $value=>$label)<flux:select.option value="{{ $value }}">{{ $label }}</flux:select.option>@endforeach</flux:select><label><span>{{ __('return_orders.field_line_note') }}</span><input type="text" wire:model="lineDrafts.{{ $line->id }}.note"></label></div>@endforeach</section><div class="form-actions"><flux:button href="{{ route('return-orders.show',$order) }}" variant="outline" wire:navigate>{{ __('common.cancel') }}</flux:button><flux:button type="submit" variant="primary">{{ __('return_orders.btn_save_inspect') }}</flux:button></div></form></div>
